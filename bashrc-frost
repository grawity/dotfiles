# vim: ft=sh

[[ $- != *i* ]] && return

iwps() {
	if [[ $* ]]; then
		sudo iw ${WLAN_DEV:-wlan0} set power_save "$@"
	fi
	iw ${WLAN_DEV:-wlan0} get power_save
}

unison() {
	if [[ -t 1 ]]; then
		run unison-x11 "$@"
	else
		unison-text "$@"
	fi
}

. ~/lib/dotfiles/bashrc-rain
