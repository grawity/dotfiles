#!sh

# These variables are not in environ because I don't use the programs,
# so I'm recording them in a separate file.

GNUSTEP_USER_ROOT="$XDG_CONFIG_HOME/GNUstep"
ICEWM_PRIVCFG="$XDG_CONFIG_HOME/IceWM"

# Not needed, XDG path is already the default
#LFTP_HOME="$XDG_CONFIG_HOME/lftp"

# Not needed, 'xim' is already the default
#QT_IM_MODULE

# Driver is now autodetected correctly
#VDPAU_DRIVER="r600"
