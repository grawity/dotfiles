" By default, the ':' separator in <ns:Tag> is formatted as Comment,
" and Comments are formatted italic in Zenburn, and this just looks
" ugly and awkward (especially in Windows Terminal).
hi link xmlAttribPunct xmlNamespace
