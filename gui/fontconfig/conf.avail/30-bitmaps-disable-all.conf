<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
	<!-- Disable embedded bitmaps globally -->

	<match target="font">
		<edit name="embeddedbitmap">
			<bool>false</bool>
		</edit>
	</match>

</fontconfig>
