# vim: ft=sh

_bgpath() {
	local f=~/bin/bird-bgpath
	if [[ -e $f ]]; then
		perl $f
	else
		ssh wolke "perl $f"
	fi
}

#//() { sudo vtysh -c "$*"; }
#//path() { sudo vtysh -c "show ip bgp $1" | _bgpath; }

//() { sudo birdc "$@"; }
//path() { sudo birdc show route all "$@" | _bgpath; }
