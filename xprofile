#!sh

ulimit -c 65536

#spawn -l- acpieventd

if [ -e ~/.mpdconf ]; then
	spawn -Ll- mpd
fi

#spawn mpc-up
#spawn scmpc
#spawn mc-presence available

if [ -f ~/.config/synclient.conf ]; then
	xargs synclient < ~/.config/synclient.conf
fi
